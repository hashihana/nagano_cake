<div class="container">
      <div class="row">

<h2>商品新規登録</h2>

 <%= render 'items/error', model: @item %>

  <%= form_with model: @item, url: admin_items_path do |f| %>
  
  <div class="col-md-6">

      <div class="form-group">
      <%= f.label :image, "商品画像", class:"col-md-2" %>
      <%= f.file_field :image, accept: "image/*" %>
      </div>

      <div class="form-group">
      <%= f.label :name, "商品名", class:"col-md-2"  %>
      <%= f.text_field :name, size:"56x5" %>
      </div>

      <div class="form-group">
     <%= f.label :name, "商品説明", class:"col-md-2"   %>
      <%= f.text_area :introduction, size:"55x5" %>
      </div>

      <div class="form-group">
      <%= f.label :price, "税抜価格", class:"col-md-2"   %>
      <%= f.text_field :price, size:"56x5" %>円
      </div>

      <div class="form-group">
      <%= f.label :is_active, "販売ステータス", class:"col-md-3"  %>
      <%= f.select :is_active,[["販売中",true],["販売停止中",false]],include_blank: "選択して下さい" %>
      </div>

     <%= f.submit "新規登録", class: "btn btn-secondary" %>
            <% end %>
  
        <div class="col-md-2"></div>
    </div>
</div>