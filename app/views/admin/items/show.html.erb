<h1>商品詳細</h1>

<div>
  <%= image_tag @item.get_image(250,250) %>
</div>
<div>
   <p>商品名<%= @item.name %></p>
</div>

<div>
   <p>商品説明<%= @item.introduction %></p>
</div>
<div>
  <p>ジャンル<%= @item.genre.name %></p>
</div>

<div>
   <p>税込価格(税抜価格)<%= @item.introduction %></p>
</div>

<div>
      <p>販売ステータス</p>
      <% if @item.is_active == true %>
        <%= "販売中" %>
      <% else %>
        <%=  "販売停止中" %>
      <% end %>
      <br>
  </div>
  <div>
 <%= link_to "編集する",edit_admin_item_path(item.id) %>
 </div>