
<div class="container">
  <div class="row">
    <div class="col-md-3">
    </div>

    <div class="col-md-9">
      <div class="top-box">
        <p class="top-text">
          ようこそ、ながのCakeへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。<br>
        </p>
      </div>

      <div class="recommend-title">
        <h3>新着商品</h3>
      </div>
      <div class="recommend-container d-flex flex-row">
        <% @random.each do |item| %>
          <div class="recommend-product">
  		        <% if item.is_active == true %>
            <%= link_to item_path(item) do %>
              <!--<%#= attachment_image_tag item, :image, fallback: 'no_image.jpg', size:'180x130' %><br>-->
              <p><%= image_tag item.get_image(180,130) %></p>
               <% end %>
            <p><%= item.name %></p>
            <p>¥<%= (item.price*1.1).floor.to_s(:delimited)%>(税込)</p>
        <% end %>
          </div>
        <% end %>
      </div>
      <div class="recommend-more"><%= link_to "全ての商品を>>", items_path %></div>
    </div>
  </div>
</div>